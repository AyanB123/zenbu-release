{
  "name": "@zenbu/cm-image-paste",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Intercepts paste events with image payloads in the composer, persists bytes via createBlob, populates the renderer image cache, and inserts an `@blob:<id>` pill token at the caret. Contributes a `cm.composer-extension` to every composer.",
  "exports": {
    ".": "./zenbu.plugin.ts"
  },
  "dependencies": {
    "@codemirror/state": "^6.0.0",
    "@codemirror/view": "^6.0.0",
    "@zenbu/app": "workspace:*",
    "@zenbujs/core": "^0.0.27",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.19.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "typescript": "^5.6.0"
  }
}
